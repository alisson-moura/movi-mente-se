# Banco de dados
DB_URL=jdbc:postgresql://localhost/movimente-se
DB_USERNAME=admin
DB_PASSWORD=

# Connection pool (opcional — valores padrão já definidos no application.properties)
# DB_POOL_MAX=10
# DB_POOL_MIN_IDLE=5