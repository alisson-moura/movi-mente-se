info:
  name: Renova Sessão
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/alunos/sessoes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
